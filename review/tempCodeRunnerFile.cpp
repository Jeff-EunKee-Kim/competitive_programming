
    // cout << grid[row][col] << " "; 
    // if (grid[row][col] == 2) {
    //     cout << endl; 
    //     cout << "We found 2! "<< endl;
    // }
    // dfs(grid, row-1, col, visited);
    // dfs(grid, row+1, col, visited);
    // dfs(grid, row, col+1, visited);
    // dfs(grid, row, col-1, visit