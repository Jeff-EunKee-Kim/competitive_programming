#include <iostream>
typedef long long ll;
typedef vector<int> vi;
typedef pair<int,int> pi;

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    // Speeds up input and output 
    // \n is faster than endl because endl always cause a flush operation 
    // string s;
    // getline(cin, s);
    //If the amount of data is unknown, the following loop is useful:
    // while (cin >> x) {
    // }


    cout << "Hello to my repo" << endl; 
}